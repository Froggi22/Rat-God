export { type Bot, type Embed, Intents, type Interaction, InteractionResponseTypes, type User } from "discordeno"
export { Application, Router } from "oak"

export type EmbedFile = {
	blob: unknown
	name: string
}
